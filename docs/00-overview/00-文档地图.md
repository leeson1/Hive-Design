# 00 文档地图

## 目的

说明 Hive 设计文档的阅读路径、维护方法、版本治理方式。

## 当前阶段

- 当前文档阶段：`Reference Architecture Draft v0.3`
- 本阶段重点：组件边界、一致性边界、能力矩阵、锁治理、端到端时序

## 总览架构图

### Hive Overall Architecture

```mermaid
flowchart TD
    Q["Queen\n需求方向 / 关键裁决"]
    D["Orchestrator\n调度 / 状态推进 / 决策分流"]
    KB["Hive Knowledge Base\n外部状态 / 共享知识"]

    I["Idea"]
    B["Brief"]
    P["Plan"]
    PH["Phase"]
    T["Task"]

    W1["Worker Bee A\nClaude Code / Codex"]
    W2["Worker Bee B\nClaude Code / Codex"]
    W3["Worker Bee C\nClaude Code / Codex"]

    H["Handoff"]
    DEC["Decision"]
    ISS["Issue / Blocker"]
    CP["Checkpoint"]

    Q -->|提出需求 / 确认方向| I
    Q -->|关键裁决| D

    I --> B --> P --> PH --> T
    P -->|计划状态| KB
    PH -->|阶段状态| KB
    T -->|任务状态| KB

    D -->|读取状态 / 调度任务| KB
    D -->|派发 Task| W1
    D -->|派发 Task| W2
    D -->|派发 Task| W3

    T -->|Task 引用| W1
    T -->|Task 引用| W2
    T -->|Task 引用| W3

    W1 -->|完成写回| H
    W2 -->|完成写回| H
    W3 -->|完成写回| H

    W1 -->|发现阻塞| ISS
    W2 -->|发现阻塞| ISS
    W3 -->|发现阻塞| ISS

    H --> KB
    ISS --> KB
    D -->|生成决策| DEC
    D -->|写入 Checkpoint| CP
    DEC --> KB
    CP --> KB
    KB -->|反馈状态 / 结果| D
    D -->|Requirement conflict 升级| Q
```

图示要点：

- Queen 提供需求方向与关键决策。
- Orchestrator 负责读取 Hive Knowledge Base、派发任务、推进状态、分流冲突。
- Worker Bees 执行具体 Task，并写回 Handoff、Issue 与 Artifact。
- Orchestrator 基于运行结果写出 Checkpoint。
- Hive Knowledge Base 承载 Plan、Phase、Task 及其运行结果，是系统外部状态来源。

## 读者角色

- 产品/业务决策者（关注目标与边界）
- 架构设计者（关注状态模型与约束）
- 执行代理开发者（关注任务输入输出）
- 运营维护者（关注恢复与可观测性）

## 推荐进入路径

1. 先读 `01-Hive-Overall-Architecture.md`
2. 再读 `02-Reference-Architecture.md`
3. 再读 `06-coordination/02-Consistency-and-Transaction-Boundaries.md`
4. 再读 `07-reliability/06-Orchestrator-Reconcile-Loop.md`
5. 最后用 `07-reliability/09-End-to-End-Sequence-Scenarios.md` 和 `08-appendix/10-State-Transition-Tables.md` 校验整体行为

## 版本策略

- 主版本：架构原则变化（例如 v1.0 -> v2.0）
- 次版本：章节结构或关键规则增强（例如 v0.1 -> v0.2）
- 修订版本：文字澄清、示例补充（例如 v0.2.1）

## 维护原则

- 规则优先于示例
- 状态优先于叙述
- 变更必须可追溯（Decision + Checkpoint）
- 协议优先于自由 prompt
