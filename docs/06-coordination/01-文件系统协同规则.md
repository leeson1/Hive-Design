# 01 文件系统协同规则

## Rule 1：目录语义隔离

建议目录：

- /ideas
- /briefs
- /plans
- /phases
- /tasks
- /decisions
- /issues
- /handoffs
- /checkpoints

禁止混存。

## Rule 2：单一事实源

- Task 状态只在一个字段维护。
- 禁止从文件名推断状态。

## Rule 3：显式状态变更

- 完成必须显式更新状态字段。

## Rule 4：归档策略

- 已完成任务定期归档，避免噪音无限增长。
